version: '3.1'

services:

  pgvector:
    image: ankane/pgvector
    environment:
      POSTGRES_USER: mock_user
      POSTGRES_PASSWORD: mock_password
      POSTGRES_DB: mock_db
    ports:
      - 5432:5432